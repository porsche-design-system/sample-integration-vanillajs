{
  "name": "sample-integration-vanillajs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "serve",
    "build": "mkdir -p build && rm -rf ./build/* && mkdir -p build/node_modules && cp index.html ./build/ && cp -R ./node_modules/@porsche-design-system ./build/node_modules/ && cp -R ./node_modules/@porschehn ./build/node_modules/",
    "prettier": "prettier --write '**/*.html'"
  },
  "dependencies": {
    "@porsche-design-system/components-js": "^1.5.2",
    "@porschehn/navigation": "^1.4.2"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "serve": "^11.3.2"
  }
}
