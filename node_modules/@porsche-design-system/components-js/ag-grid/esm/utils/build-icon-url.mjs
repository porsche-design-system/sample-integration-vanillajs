import { ICONS_MANIFEST } from '../assets/projects/icons/dist/index.mjs';
import { getCDNBaseURL } from './get-cdn-base-url.mjs';

const DEFAULT_ICON_NAME = 'question';
const buildIconUrl = (iconName) => {
    const iconFileName = ICONS_MANIFEST[iconName];
    return iconFileName ? `${getCDNBaseURL()}/icons/${iconFileName}` : buildIconUrl(DEFAULT_ICON_NAME);
};

export { buildIconUrl };
