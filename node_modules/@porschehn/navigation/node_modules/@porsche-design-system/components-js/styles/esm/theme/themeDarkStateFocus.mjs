import { themeLightStateFocus } from './themeLightStateFocus.mjs';

const themeDarkStateFocus = themeLightStateFocus; // it's important that focus color is the same for light and dark theme

export { themeDarkStateFocus };
