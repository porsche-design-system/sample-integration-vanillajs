import { F as FOCUSABLE_ELEMENTS_SELECTOR, r as getHTMLElement } from './helper-baaa661e.js';

function getAllFocusableElements(element) {
  const htmlElements = [];
  element === null || element === void 0 ? void 0 : element.querySelectorAll(FOCUSABLE_ELEMENTS_SELECTOR).forEach((element) => {
    const htmlElement = getHTMLElement(element);
    if (htmlElement !== null) {
      htmlElements.push(htmlElement);
    }
  });
  return htmlElements;
}

export { getAllFocusableElements as g };

//# sourceMappingURL=helper-530753b6.js.map