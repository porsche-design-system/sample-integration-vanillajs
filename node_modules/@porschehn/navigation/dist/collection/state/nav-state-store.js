import { createStore } from "@stencil/store";
import { getEnvironment } from "../entities/environment";
import { DEFAULT_ENVIRONMENT, DEFAULT_LOCALE, DEFAULT_UNREAD_MESSAGES_COUNT, LOGGED_OUT_USER_STATUS } from "../utility/constants";
import { Application } from "../utility/constants/app";
export const navStateStore = createStore({
  loggedInState: LOGGED_OUT_USER_STATUS,
  unreadMessagesCount: DEFAULT_UNREAD_MESSAGES_COUNT,
  userConsent: false,
  env: getEnvironment(DEFAULT_ENVIRONMENT),
  app: Application.default,
  usingKeyboard: false,
  locale: DEFAULT_LOCALE
});
//# sourceMappingURL=nav-state-store.js.map
