import { Fragment, h } from "@stencil/core";
import { IsLoggedIn } from "../../utility/helper";
import { navStateStore } from "../../state/nav-state-store";
export default function Login({ content, showSkeleton }) {
  const { login, more, signup, signupText } = content;
  const { loggedInState } = navStateStore.state;
  const isLoggedIn = IsLoggedIn(loggedInState);
  if (isLoggedIn) {
    return null;
  }
  return (h("div", { class: "myporsche-login" }, showSkeleton ? (h(Fragment, null, h("div", { class: "skeleton-login-container" }, h("div", { class: "skeleton-login" }), h("div", { class: "skeleton-more" })), h("div", { class: "skeleton-signup-container" }, h("div", { class: "skeleton-signup" }, content.signupText)))) : null, h("phn-myporsche-login", { login: login, more: more, signup: signup, signupText: signupText, style: { display: showSkeleton ? 'none' : '' } })));
}
//# sourceMappingURL=myporsche-drawer-login.js.map
