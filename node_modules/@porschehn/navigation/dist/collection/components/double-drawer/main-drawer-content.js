import { h } from "@stencil/core";
import { navContentStore } from "../../state/nav-content-store";
import { isString } from "../../utility/helper";
export const MainDrawerContent = ({ activeRoutingKey, locale, breakpoint, backButtonText, onClickBack, receivedContent }) => (h("div", { class: "drawer-content" }, h("div", { class: "main-drawer-header" }, isString(backButtonText) ? (h("phn-back-button", { class: "back-button", onClick: onClickBack, text: backButtonText, analyticsId: navContentStore.state.menu.id, activeRoutingKey: activeRoutingKey })) : null), h("phn-router", { activeRoutingKey: activeRoutingKey, breakpoint: breakpoint, locale: locale, receivedContent: receivedContent })));
//# sourceMappingURL=main-drawer-content.js.map
