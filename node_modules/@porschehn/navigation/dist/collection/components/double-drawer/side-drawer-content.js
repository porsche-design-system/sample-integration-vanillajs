import { h } from "@stencil/core";
import { navContentStore } from "../../state/nav-content-store";
import { isString } from "../../utility/helper";
export const SideDrawerContent = ({ activeRoutingKey, locale, backButtonText, onClickBack }) => {
  return (h("div", { class: "drawer-content" }, h("span", { class: "first-focus", tabIndex: -1, "aria-hidden": "true" }), h("div", { class: "side-drawer-header" }, isString(backButtonText) ? (h("phn-back-button", { class: "back-button", onClick: onClickBack, text: backButtonText, analyticsId: navContentStore.state.menu.id, activeRoutingKey: activeRoutingKey })) : null), h("phn-side-drawer", { activeRoutingKey: activeRoutingKey, locale: locale })));
};
//# sourceMappingURL=side-drawer-content.js.map
