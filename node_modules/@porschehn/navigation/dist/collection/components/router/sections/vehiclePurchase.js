import { getMenuContent } from "../../../services/menu-items-service";
import { navContentStore } from "../../../state/nav-content-store";
import { h } from "@stencil/core";
import { isDesktop } from "../../../utility/helper";
export default function VehiclePurchaseSection({ activeRoutingKey, topLevelRoutingKey, breakpoint, receivedContent }) {
  const showHeadline = !isDesktop(breakpoint);
  const displayedContent = getMenuContent(navContentStore.state, activeRoutingKey, topLevelRoutingKey);
  /* istanbul ignore if */
  if (displayedContent === null) {
    return h("div", null);
  }
  return h("phn-menu", { item: displayedContent, showHeadline: showHeadline, receivedContent: receivedContent });
}
//# sourceMappingURL=vehiclePurchase.js.map
