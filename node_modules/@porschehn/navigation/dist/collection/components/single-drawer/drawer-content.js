import { Fragment, h } from "@stencil/core";
import { navContentStore } from "../../state/nav-content-store";
import { showBackButton } from "../../services/routing-manager";
export default function DrawerContent({ routingKey, breakpoint, locale, receivedContent }) {
  const { menu } = navContentStore.state;
  return (h(Fragment, null, h("div", { class: "bottom-gradient" }), h("div", { class: "drawer-content" }, h("phn-drawer-header", { showBackButton: showBackButton(routingKey), backButtonAnalyticsId: menu.id, breakpoint: breakpoint, route: routingKey, receivedContent: receivedContent, locale: locale }), h("phn-router", { activeRoutingKey: routingKey, breakpoint: breakpoint, locale: locale, receivedContent: receivedContent }))));
}
//# sourceMappingURL=drawer-content.js.map
