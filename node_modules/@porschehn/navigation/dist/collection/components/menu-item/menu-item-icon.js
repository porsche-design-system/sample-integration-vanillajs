import { h } from "@stencil/core";
import { isString } from "../../utility/helper";
export default function Icon({ isLoading, icon }) {
  if (isLoading) {
    return (h("div", { class: "icon-container" }, h("phn-p-spinner", { color: "inherit", size: "inherit" })));
  }
  if (isString(icon)) {
    return (h("div", { class: "icon-container skeleton-item" }, h("phn-p-icon", { color: "inherit", size: "inherit", source: icon }), h("slot", { name: "icon-modifier" })));
  }
  return null;
}
//# sourceMappingURL=menu-item-icon.js.map
