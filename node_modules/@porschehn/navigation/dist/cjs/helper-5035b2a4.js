'use strict';

const helper = require('./helper-67121a1c.js');

function getAllFocusableElements(element) {
  const htmlElements = [];
  element === null || element === void 0 ? void 0 : element.querySelectorAll(helper.FOCUSABLE_ELEMENTS_SELECTOR).forEach((element) => {
    const htmlElement = helper.getHTMLElement(element);
    if (htmlElement !== null) {
      htmlElements.push(htmlElement);
    }
  });
  return htmlElements;
}

exports.getAllFocusableElements = getAllFocusableElements;

//# sourceMappingURL=helper-5035b2a4.js.map