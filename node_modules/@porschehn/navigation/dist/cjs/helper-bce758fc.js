'use strict';

const naviError = require('./navi-error-3887a972.js');

function getAllFocusableElements(element) {
  const htmlElements = [];
  element === null || element === void 0 ? void 0 : element.querySelectorAll(naviError.FOCUSABLE_ELEMENTS_SELECTOR).forEach((element) => {
    const htmlElement = naviError.getHTMLElement(element);
    if (htmlElement !== null) {
      htmlElements.push(htmlElement);
    }
  });
  return htmlElements;
}

exports.getAllFocusableElements = getAllFocusableElements;

//# sourceMappingURL=helper-bce758fc.js.map