import { Content } from '../entities/content';
import { ApplicationType } from '../utility/constants/app';
export declare const contentCache: Map<string, Content>;
export declare function fetchContent(locale: string, app?: ApplicationType): Promise<Content>;
export declare function constructFlagURL(footerAssetsUrl: string, locale: string): string;
export declare function getFlagURL(footerAssetsUrl: string, locale: string): Promise<string>;
