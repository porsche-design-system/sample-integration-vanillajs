import { Config } from '../entities/config';
type UserData = {
  firstName?: string;
  lastName?: string;
  porscheId?: string;
};
export declare function getUserNameFromUserData(userData: UserData): string;
export declare function getUserIdFromUserData(userData: UserData): Promise<string>;
export declare const userDataCache: Map<string, UserData>;
export declare function initializeUserData(accessToken: string, locale: string, config: Config): Promise<void>;
export declare function initAuthIframe(config: Config): void;
export {};
