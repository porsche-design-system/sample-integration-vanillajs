import { IAbTestingService } from './ab-testing-service.interface';
import { AvailableExperiment, Experiment, MatomoClient } from '../../utility/constants/ab-testing';
import { TMenu } from '../../entities/content';
export declare class MatomoAbTestingService implements IAbTestingService {
  client: MatomoClient | null;
  experiments: Experiment[];
  init(): Promise<void>;
  execute(experimentName: AvailableExperiment): void;
  executeContentChange(menu: TMenu): TMenu[];
  private setClient;
  private loadExperiments;
  loadContentGeneratorExperiments(): Promise<void>;
  private getExperiment;
  private getVariation;
  private getClient;
  private injectScript;
  private getAbTestingDefinitions;
  private getMatomoScript;
}
export declare const MatomoAbTesting: MatomoAbTestingService;
