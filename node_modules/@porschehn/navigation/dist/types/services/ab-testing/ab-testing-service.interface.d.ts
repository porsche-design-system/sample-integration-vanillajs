import { AvailableExperiment } from '../../utility/constants/ab-testing';
import { TMenu } from '../../entities/content';
export interface IAbTestingService {
  init(): Promise<void>;
  execute(name: AvailableExperiment): void;
  executeContentChange(menu: TMenu): TMenu[];
  loadContentGeneratorExperiments(): Promise<void>;
}
