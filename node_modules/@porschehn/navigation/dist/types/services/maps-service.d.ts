import { LatLong, Prediction } from '../entities/places';
import { NaviErrorType } from '../utility/navi-error';
export declare class MapsService {
  private autocompleteService;
  private placesService;
  private geocoderService;
  private getLocation;
  init(apiKey: string): Promise<void>;
  getPredictions(input: string, locale: string): Promise<Prediction[]>;
  getPlaceCoordinates(placeId: string): Promise<LatLong>;
  getFormattedAddressFromCoordinates(latLong: LatLong): Promise<string>;
}
export declare function getErrorType(status: string): NaviErrorType;
declare const _default: MapsService;
export default _default;
