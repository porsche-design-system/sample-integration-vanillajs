import { Environment } from '../entities/environment';
export declare const LOGGING_ENDPOINT = "https://logs.nav.porsche.com";
declare class Logger {
  private env;
  private version;
  private breakpoint;
  private locale;
  init(env: Environment, breakpoint: number, locale: string): void;
  private log;
  info(id: string, message?: string): void;
  error(id: string, err: Error): void;
}
declare const _default: Logger;
export default _default;
