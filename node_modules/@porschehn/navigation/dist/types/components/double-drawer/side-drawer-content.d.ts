import { JSX } from '../../stencil-public-runtime';
export declare const SideDrawerContent: ({ activeRoutingKey, locale, backButtonText, onClickBack }: {
  activeRoutingKey: string;
  locale: string;
  backButtonText: string;
  onClickBack: () => void;
}) => JSX.Element;
