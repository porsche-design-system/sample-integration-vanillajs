import { JSX } from '../../stencil-public-runtime';
export declare const MainDrawerContent: ({ activeRoutingKey, locale, breakpoint, backButtonText, onClickBack, receivedContent }: {
  activeRoutingKey: string;
  locale: string;
  breakpoint: number;
  backButtonText: string;
  onClickBack: () => void;
  receivedContent: boolean;
}) => JSX.Element;
