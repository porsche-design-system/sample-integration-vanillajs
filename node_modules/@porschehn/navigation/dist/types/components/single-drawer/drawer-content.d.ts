import { JSX } from '../../stencil-public-runtime';
export default function DrawerContent({ routingKey, previousRoutingKey, breakpoint, locale, showBackButton, receivedContent }: {
  routingKey: string;
  previousRoutingKey: string;
  breakpoint: number;
  locale: string;
  showBackButton: boolean;
  receivedContent: boolean;
}): JSX.Element;
