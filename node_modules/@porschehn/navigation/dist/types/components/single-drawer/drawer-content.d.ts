import { JSX } from '../../stencil-public-runtime';
export default function DrawerContent({ routingKey, breakpoint, locale, receivedContent }: {
  routingKey: string;
  breakpoint: number;
  locale: string;
  receivedContent: boolean;
}): JSX.Element;
