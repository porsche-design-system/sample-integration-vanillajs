import { EventEmitter, JSX } from '../../stencil-public-runtime';
import { DrawerStatusTypes } from '../../utility/constants/app';
export declare class ContextualDrawer {
  openBackdrop: EventEmitter<boolean>;
  isDrawerOpened: boolean;
  phnContextualDrawerStatus: EventEmitter<{
    status: DrawerStatusTypes;
  }>;
  closeContextualDrawer(): void;
  openContextualDrawer(): void;
  private closeDrawer;
  componentDidLoad(): void;
  render(): JSX.Element;
}
