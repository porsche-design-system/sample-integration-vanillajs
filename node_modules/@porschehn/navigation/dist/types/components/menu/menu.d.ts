import { EventEmitter, JSX } from '../../stencil-public-runtime';
import { TMenu, TVehiclePurchaseMenu } from '../../entities/content';
export declare class Menu {
  item: TMenu | TVehiclePurchaseMenu;
  showHeadline: boolean;
  receivedContent: boolean;
  showSkeleton: boolean;
  el: HTMLPhnMenuElement;
  mobileMenuItemClick: EventEmitter<string[]>;
  componentWillLoad(): void;
  private savedVehiclesPosition;
  private waitForComponentsToLoad;
  private showMenu;
  private getSavedVehiclesContent;
  render(): JSX.Element;
}
