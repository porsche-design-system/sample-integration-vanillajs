import { JSX } from '../../stencil-public-runtime';
export default function MenuItemText({ text, size, subtext, fontWeight }: {
  text: string;
  size: string;
  subtext: string;
  fontWeight: string;
}): JSX.Element;
