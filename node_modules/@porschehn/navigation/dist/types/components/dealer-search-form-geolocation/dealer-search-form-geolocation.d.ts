import { EventEmitter } from '../../stencil-public-runtime';
import { Prediction } from '../../entities/places';
export declare class DealerSearchFormGeolocation {
  locale: string;
  predictions: Prediction[];
  errorMessage: string | null;
  formErrorMessage: string | null;
  isLoading: boolean;
  showGeolocationButton: boolean;
  animateResults: boolean;
  searchTerm: string;
  showSkeleton: boolean;
  dealerSearchFormEvent: EventEmitter;
  clearSearchEvent: EventEmitter;
  removeSkeletonOnDealerSearchForm: EventEmitter;
  el: HTMLPhnDealerSearchFormGeolocationElement;
  private debounceTimeout;
  loadedAutoCompleteListener(): void;
  private waitForComponentsToLoad;
  disconnectedCallback(): void;
  componentWillLoad(): void;
  componentDidLoad(): void;
  private emitPageLoadEvent;
  private emitResultsLoadEvent;
  private fetchDealers;
  private onSubmit;
  private getCurrentLocation;
  private onDealerSearchEntry;
  private reset;
  render(): HTMLPhnDealerSearchFormElement;
}
