import { EventEmitter } from '../../stencil-public-runtime';
import { TCountryRecommender } from '../../entities/content';
export declare class CountryRecommender {
  locale: string;
  content: TCountryRecommender;
  flagURL: string;
  el: HTMLPhnCountryRecommenderElement;
  bannerClosed: EventEmitter<void>;
  localeWatcher(): Promise<void>;
  private setFlagURL;
  private closeCountryRecommender;
  componentWillLoad(): void;
  componentDidLoad(): void;
  handleAnimationEnd(e: AnimationEvent): void;
  private sendDataLayerEvent;
  render(): HTMLPhnCountryRecommenderElement;
}
