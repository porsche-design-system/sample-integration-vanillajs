import { ContextualIconProps, ContextualIconResponse } from '../../utility/constants/app';
export default function shopIcons({ locale, app, mode, theme, isNavigationLoaded }: ContextualIconProps): ContextualIconResponse;
