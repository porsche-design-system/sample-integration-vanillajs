import { EventEmitter } from '../../stencil-public-runtime';
import { NavigationThemeType } from '../../components';
import { RoutingKeyWithClick } from '../../entities/routing-key';
import { NavigationModeType } from '../../utility/constants';
import { ApplicationType } from '../../utility/constants/app';
export declare class Level0Icons {
  locale: string;
  theme: NavigationThemeType;
  mode: NavigationModeType;
  app: ApplicationType;
  breakpoint: number;
  isLoading: boolean;
  isNavigationLoaded: boolean;
  openDrawer: EventEmitter<RoutingKeyWithClick>;
  render(): HTMLPhnLevel0IconsElement;
}
