import { ContextualIconProps, ContextualIconResponse } from '../../utility/constants/app';
export default function pcomIcons({ locale, app, mode, theme, isNavigationLoaded }: ContextualIconProps): ContextualIconResponse;
